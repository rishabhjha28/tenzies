import React from 'react'

export default function Text() {
  return (
    <p>
        Click once on a die to freeze it at its current value. Aim: To get the same value on all the dice. Until keep Rolling
    </p>
  )
}
